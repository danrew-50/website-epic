(function(){const form=document.querySelector('[data-contact-form]');if(!form)return;const note=document.querySelector('[data-form-note]');form.addEventListener('submit',e=>{e.preventDefault();if(!form.checkValidity()){note.textContent='Please complete the required fields.';form.reportValidity();return}const data=new FormData(form);const subject=encodeURIComponent(data.get('subject')||'New project enquiry');const body=encodeURIComponent(`Name: ${data.get('name')}\nEmail: ${data.get('email')}\nBudget: ${data.get('budget')||'Not specified'}\n\n${data.get('message')}`);const to=(window.SITE_CONFIG&&window.SITE_CONFIG.email)||'hello@example.com';window.location.href=`mailto:${to}?subject=${subject}&body=${body}`;note.textContent='Your email app should open with the enquiry ready to send.'})})();
